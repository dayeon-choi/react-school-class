import React from "react";

const NavComponent = () => {
  const style = {
    backgroundColor: "black",
    color: "pink",
    fontSize: "48px",
    fontWeight: "bold",
    padding: 16,
    marginBottom: 10,
  };

  return <div style={style}>Dayeon's 실습</div>;
};

export default NavComponent;
