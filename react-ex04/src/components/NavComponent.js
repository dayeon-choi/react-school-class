import React from "react";

const NavComponent = () => {
  const style = {
    backgroundColor: "black",
    color: "pink",
    fontSize: "48px",
    fontWeight: "bold",
    padding: 16,
  };

  return <div style={style}>Dayeon's Project</div>;
};

export default NavComponent;
